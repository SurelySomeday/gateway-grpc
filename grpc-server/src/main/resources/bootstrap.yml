
grpc:
  server:
    security:
      private-key: classpath:cert.key
      certificate-chain: classpath:certificate.pem
      enabled: false
    port: 6565
spring:
  application:
    name: grpc-server
  cloud:
    nacos:
      server-addr: yxlgx.top:8848
      #配置中心
      config:
        namespace: dev
        file-extension: yaml
        enabled: true
      #服务发现
      discovery:
        namespace: dev
